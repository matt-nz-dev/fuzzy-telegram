container_commands:
    50-run-database-migrations:
        command: "sudo su ec2-user && composer.phar phinx migrate && exit"
        cwd: "/var/app/ondeck"
        ignoreErrors: false
        leader_only: true
        env:
            COMPOSER_HOME: /var/app/ondeck
